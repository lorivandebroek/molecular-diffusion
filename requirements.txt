# Core (venv): pip install -r requirements.txt
numpy>=1.24
pandas>=2.0
openpyxl>=3.1
scikit-learn>=1.3
torch>=2.0
matplotlib>=3.7
pyyaml>=6.0
pytest>=7.0

# Chemistry (PyPI package name: rdkit)
rdkit>=2023.3.1

# Graph neural networks (install after torch; see README for wheel URL if needed)
torch-geometric>=2.4
